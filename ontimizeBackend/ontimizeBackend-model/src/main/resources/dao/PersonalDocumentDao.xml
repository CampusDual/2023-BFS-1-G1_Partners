<?xml version="1.0" encoding="UTF-8"?>
<JdbcEntitySetup
        xmlns="http://www.ontimize.com/schema/jdbc"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.ontimize.com/schema/jdbc http://www.ontimize.com/schema/jdbc/ontimize-jdbc-dao.xsd"
        catalog="" schema="${mainschema}" table="personal_documents"
        datasource="mainDataSource" sqlhandler="dbSQLStatementHandler">
    <DeleteKeys>
        <Column>id</Column>
    </DeleteKeys>
    <UpdateKeys>
        <Column>id</Column>
    </UpdateKeys>
    <GeneratedKey>id</GeneratedKey>
    <Queries>
        <Query id="default">
            <ValidColumns>
                <Column>name</Column>
            </ValidColumns>
            <Sentence>
                <![CDATA[
                    SELECT
                        name
                    FROM
                        ${mainschema}.personal_documents
                ]]>
            </Sentence>
        </Query>
        <Query id="documentfiles">
            <AmbiguousColumns>
                <AmbiguousColumn name="name" prefix="df" />
                <AmbiguousColumn name="name" prefix="pd" />
            </AmbiguousColumns>
            <ValidColumns>
                <Column>name</Column>
                <Column>document_type</Column>
            </ValidColumns>
            <Sentence>
                <![CDATA[
                    SELECT
                        df.name, df.document_type
                    FROM
                        ${mainschema}.document_files df
                        JOIN ${mainschema}.personal_documents pd ON df.personal_document_id = pd.id
                        JOIN ${mainschema}.tuser u ON df.user_id = u.user_
                    #WHERE#
                ]]>
            </Sentence>
        </Query>
    </Queries>

</JdbcEntitySetup>